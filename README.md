1. Install npm, node, Visual Studio Code
2. npm init playwright

  await page.pause(); // ALLOW TO MAKE A PAUSE AND RUN PLAYWRIGHT INSPECTOR
  run tests in parralel in different browsers
  select element first
